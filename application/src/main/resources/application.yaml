spring:
  application:
    name: hexagonal-demo
  datasource:
    url: jdbc:h2:mem:hexagonal-demo;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8082